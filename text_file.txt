Simple text file.

We've staged this file, but didn't commit it. Now we're gonna change it a bit and see what 'git status' command will tell us.

Now let's see what if we modify this file, stage it, then modify it again(without staging) and then commit it.
Ok, modify it again...

Dummy changes.

One more dummy change.
